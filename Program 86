#include <stdio.h>
#include <string.h>

int main()
{
    char str[100];
    printf("Enter a line of text: ");
    scanf(" %[^\n]", str);
    
    printf("Length of string = %ld\n", strlen(str));
    
    return 0;
}

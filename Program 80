#include<stdio.h>
void fib(int n);
int main()
{
    int n;
printf("Till what number you want fibonaci series\n");
scanf("%d",&n);
fib(n);
}
void fib(int n)
{
    int sum=0,a=0,b=1;
    printf("0");
for(int i=1;n>1;n--)
{
    
    sum=a+b;
    a=b;
    b=sum;
    printf("%d",sum);
}
}
